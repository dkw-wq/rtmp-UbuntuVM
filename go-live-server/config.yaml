server:
  port: 9090
  mode: debug    # debug | release | test

srs:
  api_url: "http://localhost:1985"
  rtmp_base_url: "rtmp://localhost:1935/live"

database:
  host: localhost
  port: 5432
  user: live
  password: live_password
  dbname: live
  sslmode: disable
  timezone: Asia/Shanghai

nginx:
  hls_base_url: "http://localhost:8080/live"

auth:
  jwt_secret: "change-me-in-production"
  push_secret: "change-me-push-secret"
  push_token_expiry: "24h"
  play_secret: "change-me-play-secret"
  play_token_expiry: "24h"
  admin_username: "admin"
  admin_password: "admin123"
  admin_token_expiry: "8h"

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  cache_ttl: "1h"
  viewer_count_method: "callback"   # "callback" or "polling"
  polling_interval: "60s"
